#!/usr/bin/python3
"""creates the State class."""
from models.base_model import BaseModel


class State(BaseModel):
    """
        Attributes for the state class:
        name (str): The name of the state.
    """

    name = ""
